services:
  frontend:
    container_name: inovaskill-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: /api
    ports:
      - "80:80"
    restart: unless-stopped
    mem_limit: 256m
    cpus: 0.50
